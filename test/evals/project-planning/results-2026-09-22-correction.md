# Correction and delivery evaluation — 22 September 2026

An independent evaluator received `correction-inputs.json` and generated Critical
Boiler 2.6.7 task-orchestration context, without the implementation diff or scoring
rubric. Supplied observations were synthetic evidence; it made no live task calls,
repository edits, service starts, network requests or test-execution claims.

| Case | Observed decision |
| --- | --- |
| C1 | Assigned locator fixes to the existing tester, preserved assertion meaning, audited sibling defects, required focused reruns and claim-specific evidence reuse, then independent review and explicit integrated gates. |
| C2 | Rejected generic-scene and all-skipped evidence as proof of the delivered asset, retained negative results, prohibited repair/delegation under the supplied checkpoint, and restricted any further verification to existing authority. |
| C3 | Required exact candidate/base/merge-parent checks before integrated gates; described an append-only repair after accidental fast-forward without resetting main or rewriting existing commits. |

The evaluator also found a broad evidence-invalidation clause in the routed native
task-management reference despite resolving its intent using the detailed policy.
That ambiguity and equivalent domain-orchestrator clauses found by independent
review were corrected to reference the canonical applicability rules, then the
generated context was refreshed. These are discovered and repaired instruction
defects, not an unqualified first-pass success.

All 13 deterministic generation/managed-sync tests pass, including the existing
context word budgets. Syntax, release metadata, CLI smoke and generated skill
validation pass. Those checks establish packaging/routing consistency, not future
agent judgment. The three synthetic cases have no paired baseline and cannot
establish a causal speedup or guarantee elimination of recovery workflows.

Raw evaluator responses are retained in `correction-responses-2026-09-22.json`.
